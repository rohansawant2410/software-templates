name: push
on: 
  push:
    branches:
      - master
      - main
    tags:        
      - v*

jobs:
  call-workflow:
    uses: rohansawant2410/shared-workflows/.github/workflows/dotnet-ci-cd.yaml@main
    with:
      app: ${{values.application.split(":")[1]}}